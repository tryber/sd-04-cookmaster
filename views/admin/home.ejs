<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Admin</title>
  </head>

  <body>
    <h1>Painel Administrativo Cookmaster</h1>
    <h2>Cookmaster</h2>
    <h2>Receitas</h2>
    <button data-testid="buscar-receita">Buscar Receita</button>
    <button data-testid="nova-receita">Nova Receita</button>
    <p>Usuário atual: <%= `${user.firstName} ${user.lastName}` %></p>
    <pre><%= JSON.stringify(user, null, 4) %></pre>
    <button onclick="window.location.pathname='/logout'">Logout</button>
    <ul>

      <% recipes.forEach((recipe) => { %>
        <li><%= recipe.name %> <%= recipe.user %> <a href="#">Ver mais</a></li>
      <% }); %>

    </ul>
  </body>
</html>
