const userController = require('./userController');
const recipeController = require('./recipeController');
const userFormController = require('./userFormController');

module.exports = {
  userController,
  recipeController,
  userFormController,
};
