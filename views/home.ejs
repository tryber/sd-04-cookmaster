<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Home</title>
  </head>
  <body>
    <%- include('header', { user }) -%>
    <div>
      <button type="button" data-testid="buscar-receita" onclick="window.location.pathname='/recipes/search'">Buscar Receita</button>
      <h1>Receitas</h1>
      <!-- These buttons should only appear if the user is logged in. -->
      <% if(user) {%>
        <button onclick="window.location.pathname='/recipes/new'" data-testid="nova-receita" >
          Nova Receita
        </button>
        <button onclick="window.location.pathname='/me/recipes'" data-testid="minhas-receitas" >
          Minhas Receitas
        </button>
        <button onclick="window.location.pathname='/me/edit'" data-testid="minha-conta" >
          Minha Conta
        </button>
      <% } %>
      <!-- Showing recipes and database user-->
      <% getAllRecipes.forEach(item => { %>
      <div>
        <p>Receita: <%= item.name %> </p>
        <p>Usuário: <%= item.user %> </p>
        <!-- I will probably have to forward to some route like: / recipes / {id}. -->
        <a href="Link">Ver mais</a>
      </div>
      <% }) %>
    </div>
  </body>
</html>
