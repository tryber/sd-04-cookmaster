<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h3>Comfirme a exclusão da receita</h3>
  <% if (mensage) { %>
    <p><%= mensage %></p>
  <% } %>
  <form action="/recipes/<%= id %>/delete" method="POST">
    <label for="password">Senha</label>
    <input type="password" name="confirmationPassword" data-testid="senha">
    <button type="submit" data-testid="confirmar">Comfirmar</button>
  </form>
  <a href="/" data-testid="cancelar">Cancelar</a>
</body>
</html>
