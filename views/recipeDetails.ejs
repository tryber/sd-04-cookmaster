<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h3>Cookmaster</h3>
    <% if (user) { %>
    <p>Usuário atual: <%= `${user.name} ${user.lastName}` %></p>
    <a href='/logout'" data-testid=" logout">Logout</a>
    <%}%>
        <% if (!user) { %>
    <a href="/login" data-testid="login">Login</a>
    <% } %>
    <div>
        <h1><%= recipes.name %></h1>
        <p>Enviada por: <%= recipes.user %></p>
        <% if (user) { %>
        <% if (user.userId === recipes.userId) { %>
        <a href="/recipes/<%= recipes.recipeId %>/edit" data-testid="editar-receita">Editar Receita</a>
        <a href="/recipes/<%= recipes.recipeId %>/delete" data-testid="excluir-receita">Excluir Receita</a>
        <%}%>
        <%}%>
    </div>
    <h2>Ingredientes</h2>
    <p><%= recipes.ingredients %></p>
    <h2>Modo de preparo</h2>
    <p><%= recipes.instructions %></p>
</body>

</html>
