<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Delete recipe</title>
  </head>
  <body>
    <%- include('header', { user }); %>
    <% console.log(user) %>
    <form action="/me/edit" method="POST">
      <h1>Cookmaster - Editar usuário</h1>
      <label for="email">Email</label>
      <input type="email" name="email" data-testid="email" value="<%= user.email %>">
      <label for="password">Senha</label>
      <input type="password" name="password" data-testid="senha">
      <label for="confirmPassword"></label>
      <input type="password" name="confirmPassword" data-testid="confirmar-senha">
      <label for="firstName">Nome</label>
      <input type="text" name="firstName" data-testid="nome" value="<%= user.name %>">
      <label for="lastName">Sobrenome</label>
      <input type="text" name="lastName" data-testid="sobrenome" value="<%= user.lastName %>">
      <a href="/">Cancelar</a>
      <button type="submit" data-testid="salvar">Salvar</button>
    </form>
  </body>
</html>
