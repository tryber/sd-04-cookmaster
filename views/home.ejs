<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', {title: 'Cookmaster'}) %>
  <body>
    <%- include('partials/navbar') %>
    <div class="container home">
      <h1>Receitas</h1>
      <div>
        <% if (user) { %>
          <a class="button" href="/" data-testid="nova-receita">Nova Receita</>
        <% } %>
        <a class="button" href="/" data-testid="buscar-receita">Buscar Receita</>
      </div>
    </div>
    <div class="container-cards">
      <% for (let i = 0; i <= data.length-1; i++ ) { %> <%- include('partials/recipeCard', {i}) %>
      <% } %>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
