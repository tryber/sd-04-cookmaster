<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cookmaster - Delete Recipe</title>
</head>
<body>
    <h1>Deletar Receita</h1>
    <% if (user) { %>
        Olá, <%= user.name %>, confirme sua senha para excluir essa receita.
    <% } else { %>
        <h3> ERRO </h3>
    <% } %>
    <% if(message) { %>
    <span>
        <strong><%= message %></strong>
    </span>
    <% } %>

    <form action="/recipes/<%= id %>/delete" method="post">
        <label for="password">Senha:</label>
        <input data-testid="senha" type="password" name="password" id="password">
        <button data-testid="cancelar" type="reset">Cancelar</button>
        <button data-testid="confirmar" type="submit">Confirmar</button>
    </form>
</body>
</html>