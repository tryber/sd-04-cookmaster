<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Login</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <% if (message) {%>
      <div class="card">
        <div class="card-body">
          <div class="message"><%= message %></div>
          <% } %>
          <form action="/login" method="POST">
            <label for="emailInput">Email: </label>
            <input type="text" name="email" class="col-md-6" id="emailInput" data-testid="email" /><br />
            <label for="passwordInput">Senha: </label>
            <input type="password" name="password" class="col-md-6" id="passwordInput" data-testid="senha" /><br />
            <% if (redirect) { %>
            <input type="hidden" name="redirect" value="<%= decodeURIComponent(redirect) %>" />
            <% } %>
            <button type="submit" data-testid="entrar">Entrar</button>
            <div class="col-lg" style="text-align: right">
              <a href="/cadastro" data-testid="cadastrar" class="btn btn-primary">
                  Cadastrar-se </a>
                </button>
              </a>
            </div>
        </div>
      </div>
    </form>
  </body>
</html>
