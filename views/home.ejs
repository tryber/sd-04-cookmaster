<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Home</title>
  </head>
  <body>
    <h1>Boas vindos (as) ao Cookmaster!</h1>
    <div>
      <h1>Receitas</h1>
      <ul>
        <% recipe.forEach((recipe) => { %>
          <li><%= recipe.name %></li>
          <li><%= recipe.user %></li>
          <a href="/recipes/<%= recipe.id%>">Ver mais</a>
        <% }) %>
      </ul>
    </div>
  </body>
</html>
