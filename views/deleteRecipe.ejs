<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Deletar Receita</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <% if (message) {%>
      <div class="message"><%= message %></div>
      <% } %>
    <div class="d-flex flex-column align-items-center mt-5 border">
      <h2>Corfirme a exclusão da receita</h2>
      <div class="d-flex justify-content-center">
        <form action="/recipes/<%= id %>/delete" method="POST">
          <div class="form-group">
            <div>
              <label for="passwordInput">Digite sua senha: </label>
              <input
                data-testid="senha"
                class="form-control"
                type="password"
                name="password"
                id="passwordInput"
              /><br />
            </div>
            <a href="/" class="btn btn-primary" data-testid="cancelar">Cancelar</a>
            <button class="btn btn-primary" data-testid="confirmar" type="submit">Confirmar</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
