<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cookmaster - Home</title>
</head>

<body>
  <article>
    <%- include('navbar'); %> <% if (recipes) {%>
    <h1>Receitas</h1>
    <section>
      <% recipes.forEach(recipe => { %>
      <div>
        <p> Receita: <%= recipe.name %></p>
        <p>Autor: <%= recipe.user %></p>
        <a href="/recipes/<%= recipes.id %>">Ver mais</a>
      </div>
      <%})%>
    </section>
    <%}%>
  </article>
</body>

</html>
